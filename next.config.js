/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // images :{
  //   domains: ['www.viva-media.ca'],<-- para alojar images de next se tiene q declarar el dominio
  // }
}

module.exports = nextConfig
